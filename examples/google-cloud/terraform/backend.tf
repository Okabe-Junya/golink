# This file configures the terraform backend.
# Uncomment these lines and update values when you want to use a remote backend

# terraform {
#   backend "gcs" {
#     bucket  = "golink-terraform-state"  # 既存のGCSバケット名を指定
#     prefix  = "terraform/state"
#   }
# }
